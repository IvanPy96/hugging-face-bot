<div align="center">

# 🤖 HF Monitor Bot

**Telegram-бот для мониторинга AI-моделей на Hugging Face**

AI-уведомления, саммари, `/battle`-режим и саркастичный ассистент в одном боте.

[![Python 3.11+](https://img.shields.io/badge/python-3.11+-blue.svg)](https://python.org)
[![License: MIT](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![Style: Ruff](https://img.shields.io/badge/style-ruff-black.svg)](https://github.com/astral-sh/ruff)

[🇷🇺 RU](#-ru) · [🇬🇧 EN](#-en)

</div>

---

## 🇷🇺 RU

### ✨ Что это

Бот следит за выбранными организациями на Hugging Face, присылает уведомления о новых моделях, делает AI-саммари и помогает быстро разобраться с моделью через чат.

### 🚀 Возможности

- 🔔 **Автомониторинг HF**: новые модели сразу в Telegram
- 🧠 **AI-саммари** по README модели
- 🖥️ **Оценка деплоя** (`/deploy`): параметры, VRAM и ориентир по GPU
- 💬 **AI-ассистент** по моделям и новостям (опционально с Brave Search)
- ⚔️ **Battle Mode** (`/battle`): викторина против GigaChat
- 🎯 **Важно:** вопросы для `/battle` генерирует **Gemini 3 Pro**

### 📋 Команды

`/start`, `/help`, `/info <model_id>`, `/deploy <model_id>`, `/orgs`, `/stats`, `/random`, `/hero`, `/battle`, `/agi`

### ⚡ Быстрый старт (Docker)

```bash
git clone <repo_url>
cd hf_bot
cp .env.example .env
docker compose up -d --build
```

Логи: `docker compose logs -f`  
Остановка: `docker compose down`  

### 🛠️ Локальный запуск

```bash
python -m venv .venv
source .venv/bin/activate
pip install .
cp .env.example .env
python -m hf_bot
```

### 🔐 Переменные окружения

**Обязательные:**

- `BOT_TOKEN`
- `CHAT_ID`

**Опциональные:**

- `OPENROUTER_API_KEY`
- `BRAVE_SEARCH_API_KEY`
- `POLL_SECONDS` (default `60`)
- `HF_TIMEOUT_SECONDS` (default `10`)
- `LLM_MODEL` (default `google/gemini-2.5-flash-lite`)
- `LLM_TIMEOUT_SECONDS` (default `90`)
- `STATE_PATH` (default `data/state.json`)
- `LOG_LEVEL` (default `INFO`)

### 🧩 Архитектура (кратко)

- `clients/` — API-клиенты (HF, LLM, search, web-reader)
- `handlers.py` — команды и обработка сообщений
- `monitoring.py` — периодический мониторинг и уведомления
- `formatter.py` + `content.py` — форматирование и текстовые шаблоны
- `state.py` — JSON state с атомарной записью

### 💡 Примечание

Этот проект на **100% vibecode** с помощью **Claude Opus 4.6**.

---

## 🇬🇧 EN

### ✨ What it is

This bot monitors selected Hugging Face organizations, posts new-model alerts to Telegram, generates AI summaries, and helps you chat about models in real time.

### 🚀 Features

- 🔔 **Automatic HF monitoring** with Telegram alerts
- 🧠 **AI summaries** based on model README
- 🖥️ **Deployment estimation** (`/deploy`) with VRAM and GPU hints
- 💬 **AI assistant** for model Q&A and current news (optional Brave Search)
- ⚔️ **Battle Mode** (`/battle`) against GigaChat
- 🎯 **Important:** battle questions are generated by **Gemini 3 Pro**

### 📋 Commands

`/start`, `/help`, `/info <model_id>`, `/deploy <model_id>`, `/orgs`, `/stats`, `/random`, `/hero`, `/battle`, `/agi`

### ⚡ Quick start (Docker)

```bash
git clone <repo_url>
cd hf_bot
cp .env.example .env
docker compose up -d --build
```

Logs: `docker compose logs -f`  
Stop: `docker compose down`  

### 🛠️ Local run

```bash
python -m venv .venv
source .venv/bin/activate
pip install .
cp .env.example .env
python -m hf_bot
```

### 🔐 Environment variables

**Required:**

- `BOT_TOKEN`
- `CHAT_ID`

**Optional:**

- `OPENROUTER_API_KEY`
- `BRAVE_SEARCH_API_KEY`
- `POLL_SECONDS` (default `60`)
- `HF_TIMEOUT_SECONDS` (default `10`)
- `LLM_MODEL` (default `google/gemini-2.5-flash-lite`)
- `LLM_TIMEOUT_SECONDS` (default `90`)
- `STATE_PATH` (default `data/state.json`)
- `LOG_LEVEL` (default `INFO`)

### 🧩 Project layout

- `clients/` - external API clients
- `handlers.py` - command and message handlers
- `monitoring.py` - periodic checks and alerts
- `formatter.py` + `content.py` - text formatting and phrase banks
- `state.py` - persistent JSON state

### 💡 Note

This project is **100% vibecode**, built with **Claude Opus 4.6**.
